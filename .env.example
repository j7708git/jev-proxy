# jev-proxy 本地密鑰檔。
# 用法：複製本檔為 .env 並填入真值。`.env` 已在 .gitignore 中——
# 不要提交、不要貼進對話或截圖。
#
# 載入順序（啟動時自動）：-env 指定路徑 > ./.env > config.yaml 同目錄的 .env。
# 優先序：真實環境變數 > .env（shell 裡已設的同名變數會蓋掉檔案值）。
# 變數名稱需與 config.yaml 的 jev.api_key_env 一致。

# provider: openrouter 時必填（https://openrouter.ai/keys 申請）
OPENROUTER_API_KEY=sk-or-replace-me

# provider: typesafe 直連時改用：
# TYPESAFE_API_KEY=ts-replace-me

# 只放密鑰。非密鑰設定（port、門檻、截斷、queue…）一律在 config.yaml。
