# local secrets — keep .env out of version control (.env.example is the tracked template)
.env
*.env
!.env.example

# build artifact
/jev-proxy.exe
*.exe~

# runtime telemetry: contains full conversation replies; never commit
scores.jsonl
scores-*.jsonl

# calibration scratch runs (the certified one, calibration-v1.0.json, is tracked)
tests/report-*.json

# e2e scratch
.e2e-gw-pids
